# @package _global_

# to execute this experiment run:
# python train.py experiment=example2

defaults:
  - override /data: male_16k_norm
  - override /model: vits2_default_warmup
  - override /callbacks: default
  - override /trainer: ddp

# all parameters below will be merged with parameters from default configurations set above
# this allows you to overwrite only specified parameters

tags: ["vits2_default_warmup", "male_16k_norm", "use_sdp"]

seed: 12345

trainer:
  max_epochs: 1000
  devices: 2
  strategy: ddp_find_unused_parameters_true

model:
  net_g_cfg:
    n_vocab: 90
    use_sdp: True
    use_ext_spk_emb: True
    transformer_flow_type: "pre_conv2"
  net_dur_disc_cfg:
    use: False
  

data:
  batch_size: 8
  num_workers: 16
  use_ext_spk_emb: True